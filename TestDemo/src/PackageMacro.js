/**
 * 统一命名空间定义文件，防止重复定义。
 * 禁用game和server关键字，game和server命名空间已被框架使用。
 */
var tools = tools || {};	//工具类
var common = common || {}  //公共类

var model = model || {};	//数据模型
var vo = vo || {};	//vo模型

var init = init || {};	//登陆功能
var login = login || {};	//登陆功能
var main = main || {};		//主界面
var play = play || {}      //关卡
